# 로컬 실행용 예시. 복사해서 .env 로 쓰고 실제 값 채울 것 (.env 는 커밋 금지).
# GitHub Actions 에서는 이 값들을 저장소 Secrets 로 등록한다.

SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# 선택값 (미설정 시 기본값 사용)
LOOKBACK_HOURS=24
MAX_PER_FEED=5
SEND_WHEN_EMPTY=0
